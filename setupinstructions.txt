Environment Setup:
python3 -m venv .venv
source .venv/bin/activate
pip install notebook
pip install numpy
pip install scipy
pip install matplotlib
pip install jitcdde
pip install tqdm
pip install sympy

Usage: jupyter-notebook
